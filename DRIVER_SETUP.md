# إعداد السائق

1. أنشئ حساب السائق في Firebase Authentication (Phone أو الطريقة التي تعتمدها).
2. بعد أول تسجيل دخول، انسخ UID للسائق.
3. في Firestore أنشئ collection اسمها `drivers`.
4. أنشئ document ID = UID للسائق.
5. أضف:
   - `name`: اسم السائق
   - `phone`: رقم السائق
   - `active`: true
6. من لوحة الإدارة، عند فتح الطلب ستظهر قائمة السائقين النشطين، اختر السائق واحفظ.
7. يظهر الطلب للسائق داخل وضع السائق.

ملاحظة: التطبيق الحالي يجعل أي سائق نشط قادراً على تحديث حالة الطلبات المسندة إليه فقط.
